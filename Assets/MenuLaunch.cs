using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class MenuLaunch : MonoBehaviour
{
    // public Button buttonNext;
    // public Button buttonPrev;
    // public Transform forceImage;

    // public void setupButtons(PlayerSpawn playerSpawnScript)
    // {
    //     buttonNext.onClick.AddListener(() => playerSpawnScript.NextSpawn() );
    //     buttonPrev.onClick.AddListener(() => playerSpawnScript.PreviousSpawn() );
    // }
}
